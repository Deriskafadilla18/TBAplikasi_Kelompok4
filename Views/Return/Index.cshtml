@model IEnumerable<AplikasiPerpustakaan.Models.Return>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nama_pengembali)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tanggal_pengembalian)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id_Borrow)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ket)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nama_pengembali)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tanggal_pengembalian)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Borrow)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ket)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Return">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Return">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Return">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
