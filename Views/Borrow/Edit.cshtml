@model AplikasiPerpustakaan.Models.Borrow

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Borrow</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id_Borrow" />
            <div class="form-group">
                <label asp-for="nama_peminjam" class="control-label"></label>
                <input asp-for="nama_peminjam" class="form-control" />
                <span asp-validation-for="nama_peminjam" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="tanggal_pinjam" class="control-label"></label>
                <input asp-for="tanggal_pinjam" class="form-control" />
                <span asp-validation-for="tanggal_pinjam" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Id_Book" class="control-label"></label>
                <select asp-for="Id_Book" class="form-control" asp-items="@(new SelectList(ViewBag.listbook, "Id_Book","judul"))"></select>
                <span asp-validation-for="Id_Book" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="lama_pinjam" class="control-label"></label>
                <input asp-for="lama_pinjam" class="form-control" />
                <span asp-validation-for="lama_pinjam" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
